% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effective_df.R
\name{effective_df}
\alias{effective_df}
\title{Effective Number of Parameters and Degrees of Freedom for Penalized Fits}
\usage{
effective_df(
  x,
  pen_par_id = NULL,
  pen_diff_id = NULL,
  pen_fn = "l0a",
  eps = 0.01,
  ...
)
}
\arguments{
\item{x}{A lavaan model object, typically the return value of
\code{\link[=penalized_est]{penalized_est()}}. For such objects, the penalty specification is read
from the object's \sQuote{penalized} attribute.}

\item{pen_par_id}{Integer vector of directly penalized free-parameter
IDs, in the same order as returned by \code{lavaan::coef()}. For a fit from
\code{\link[=penalized_est]{penalized_est()}}, defaults to the value recorded in the object.}

\item{pen_diff_id}{Named list of integer matrices of free-parameter IDs
for difference penalties (same structure as in \code{\link[=penalized_est]{penalized_est()}}). For
a fit from \code{\link[=penalized_est]{penalized_est()}}, defaults to the value recorded in the
object.}

\item{pen_fn}{The penalty function: \code{"l0a"}, \code{"alf"}, or a function.
For a fit from \code{\link[=penalized_est]{penalized_est()}}, defaults to the value recorded in
the object.}

\item{eps}{The epsilon used by the built-in penalties. For a fit from
\code{\link[=penalized_est]{penalized_est()}}, defaults to the value recorded in the object.}

\item{...}{Additional arguments passed to a user-supplied \code{pen_fn}.}
}
\value{
A data frame of class \code{plavaan_efdf} with one row per penalty
component (\code{"direct penalty"}, and one row per named block of
\code{pen_diff_id}) and a final \code{"TOTAL"} row; the row names are the
component labels. Columns:
\describe{
\item{\code{npar}}{Number of (nominal) free parameters covered by the row.
For the \code{TOTAL} row, \code{length(coef(x))}.}
\item{\code{npar_effective}}{Effective number of parameters, with penalized
parameters soft-counted by their penalty values.}
\item{\code{df_saved}}{\code{npar - npar_effective}, the degrees of freedom saved
by the penalty relative to the nominal model.}
}
The data frame also carries an \code{info} attribute with \code{n_stats} (number of
sample statistics), \code{df_model} (nominal model df, possibly negative),
\code{df_model_effective}, \code{w}, \code{eps}, and \code{pen_fn}.
}
\description{
Computes the effective number of model parameters, and hence the
effective model degrees of freedom, of a penalized lavaan fit using the
soft-count property of the l0a penalty: a directly penalized parameter
with estimate \code{z} is counted as \code{l0a(z, eps) = z^2 / (z^2 + eps)}
effective parameters, which is close to 1 for large \verb{|z|} and close to 0
for small \verb{|z|}. For a difference penalty, each column contributes one
shared value (the invariance baseline) plus the l0a soft count of the
pairwise differences in that column.
}
\details{
For a fit returned by \code{\link[=penalized_est]{penalized_est()}}, any argument left at its
default (\code{NULL} for \code{pen_par_id}/\code{pen_diff_id}, \code{"l0a"} for \code{pen_fn},
\code{.01} for \code{eps}) is filled in from the object's \sQuote{penalized}
attribute; explicitly supplied values take precedence. Note that a value
equal to the default cannot be distinguished from an omitted one, so
passing, e.g., \code{eps = .01} explicitly will still be overridden by the
recorded value.

An ID that appears in both \code{pen_par_id} and a \code{pen_diff_id} matrix is a
hard error, as the parameter would be double-counted.

The effective-df interpretation is calibrated for the l0a penalty, which
takes the value 0 at the origin. With \code{pen_fn = "alf"},
\code{alf(0) = eps^0.25 != 0}, so the soft counts do not reach 0 and the
effective df should be interpreted with care. A custom \code{pen_fn} is
evaluated as if it had l0a-like 0/1 behavior; the result is approximate.

The nominal model df, \code{n_stats - length(coef(x))}, may be negative for
penalized (often under-identified) models; the effective model df,
\code{n_stats - npar_effective}, is the meaningful quantity. The number of
sample statistics \code{n_stats} is counted per group from the model's
parameter table.

Fit indices based on the effective df (chi-square, CFI, RMSEA, AIC,
BIC) are obtained with \code{fitmeasures()} on a fit from
\code{\link[=penalized_est]{penalized_est()}}, which freezes the parameters at the penalized
estimates and refits. This fit evaluation is experimental and requires
the fit to have been created with a non-\verb{\code{"none"}} \code{test}
(e.g. \code{test = "Chisq"}); with the default \code{test = "none"},
\code{fitmeasures()} is unavailable.
}
\examples{
library(lavaan)

model <- "
  dem60 =~ y1 + y2 + y3 + y4
  dem65 =~ y5 + y6 + y7 + y8
  dem60 ~~ dem65
"
fit <- cfa(model, data = PoliticalDemocracy, std.lv = TRUE, do.fit = FALSE)
pt <- parTable(fit)
load_60 <- pt$free[pt$op == "=~" & pt$lhs == "dem60"]
load_65 <- pt$free[pt$op == "=~" & pt$lhs == "dem65"]

pen <- penalized_est(
  fit, w = 0.03,
  pen_diff_id = list(loadings = rbind(load_60, load_65)),
  eps = .01,
  test = "Chisq"
)
effective_df(pen)
fitmeasures(pen, c("chisq", "df", "cfi"))

}
